image: Visual Studio 2017

branches:
  only:
  - ParsingTree

environment:
  matrix:
  - solution_name: homework4/ParsingTree/ParsingTree.sln

before_build:
 - nuget restore %solution_name%

build_script:
  - msbuild %solution_name%

test_script:
  - vstest.console /logger:Appveyor %solution_name%
